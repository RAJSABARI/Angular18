import { NumberValidationDirectiveDirectiveDirective } from './number-validation-directive-directive.directive';

describe('NumberValidationDirectiveDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberValidationDirectiveDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
